#!/usr/bin/env python
import string
 
def zhen(cmd):
      cmd = cmd.split('\r\n')
      if(cmd[1] == ''):
           ttn= cmd[0].strip()
           ttn = ttn.lower()
        # ser
        pass
      if(cmd[1]!='' and cmd[0]!=''):
         for i in range(0,len(cmd[0])):
            ttn[i]=cmd[i]
         ttn.append(cmd[1])
def getpex(ttn):
    pass
      
      
